---
import YearTop from "../components/portal/YearTop.astro";
import { CURRENT_YEAR } from "../config/site";
import { getEventsByYear } from "../utils/events";

const events = getEventsByYear(CURRENT_YEAR);
---
<YearTop year={CURRENT_YEAR} events={events} isLatest={true} />
